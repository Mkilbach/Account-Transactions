import React from "react";
import "./styles.scss";

const NavBar = () => {
    return (
        <nav className="navbar">
            <h1 className="navbar__title">MyBank</h1>
        </nav>
    );
};

export default NavBar;
